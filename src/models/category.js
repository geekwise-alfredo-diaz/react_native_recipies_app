class Category {
    constructor(id, title, color, queryTitle) {
        this.id = id;
        this.title = title;
        this.color = color;
        this.queryTitle = queryTitle
    }
}

export default Category;